#include<iostream.h>
#include<conio.h>
#include<stdio.h>
{ int search(int arr[20], size, n)
    for(int i=0, i<= size, i++)
        {if(arr[i]== n)
           return i;}
    return -1;
}
int main()
 { printf(enter the the no. of elements in the array");
   scanf("%d%, size);
 printf("enter the elements of array")
  for(i=0,i<size, i++)
      scanf(arr[i]);
printf("enter the element to be searched for");
scanf("%d", n);
}
return 0;
}
